syntax = "proto3";

package log_aggregator.schema;

message FileCommands {
  message AddFile {
    uint32 id = 1;
    string path = 2;
  }

  message CloseFile {
    uint32 id = 1;
  }

  oneof command_type {
    AddFile add_file = 1;
    CloseFile close_file = 2;
  }
}
